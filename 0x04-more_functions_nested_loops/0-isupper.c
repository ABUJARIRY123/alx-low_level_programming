#include "main.h"
/**
 * _isupper - This program checks if a letter is upper
 *@l: the letter to be checked
 * Return: 1 for uppercase, 0 else
*/
int _isupper(int l)
{
if (l >= 65 && l <= 90)
{
return (1);
}
return (0);
}
