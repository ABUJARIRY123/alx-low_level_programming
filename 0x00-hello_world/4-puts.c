#include <stdio.h>
/**
* main - This is a program that utilizes puts to print a line
* Return: 0 (The program will return zero when it has successfully run)
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
