#include <stdio.h>
/**
* main - a program that utilizes printf to print a line
* Return:0 (upon successful execution)
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
